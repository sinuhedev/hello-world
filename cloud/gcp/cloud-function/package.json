{
  "name": "cloud-function",
  "description": "",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "scripts": {
    "dev": "node --inspect node_modules/.bin/functions-framework --target=helloWorld --port=8080 --source src",
    "build": "gcloud builds submit",
    "clean": "rm -r package-lock.json node_modules"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.3"
  }
}
