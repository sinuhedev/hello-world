steps:
- name: gcr.io/cloud-builders/gcloud
  entrypoint: bash
  args:
    - -c
    - | 
      gcloud run deploy dev-api --source . --allow-unauthenticated --region us-central1 --quiet
      echo 'Done!'
