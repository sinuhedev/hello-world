{
  "name": "nodejs",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "node": ">25"
  },
  "type": "module",
  "imports": {
    "#utils": "./src/utils/index.js"
  },
  "scripts": {
    "dev": "node --inspect --watch src/index.js",
    "clean": "rm -fr node_modules package-lock.json pnpm-lock.yaml .coverage",
    "test": "_(){ if [ $# -eq 1 ];then node --test --test-name-pattern=$1 ; else node --test  ;fi ;};_",
    "testd": "_(){ node --inspect-brk --test --test-name-pattern=$1 ;};_",
    "coverage": "node --test --experimental-test-coverage"
  }
}
