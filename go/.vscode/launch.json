{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "go",
      "name": "Debug",
      "request": "launch",
      "mode": "auto",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/cmd/server",
      "envFile": "${workspaceFolder}/.env",
    }
  ]
}